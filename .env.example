APP_NAME="AI Support Ticket Triage API"
APP_ENV=local
DATABASE_URL=sqlite+aiosqlite:///./ai_ticket_triage.db

# fake (default), ollama, or openai_compatible
TRIAGE_PROVIDER=fake

# Required only for ollama/openai_compatible.
# Examples are placeholders; do not commit real credentials.
PROVIDER_MODEL=
PROVIDER_BASE_URL=
PROVIDER_TIMEOUT_SECONDS=10
PROVIDER_API_KEY=
